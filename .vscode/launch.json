{
    // Paso 0: Colocar los breakpoints en el código.
    // Paso 1: Ejecutar el comando "npm start".
    // Paso 2: Menú lateral -> "Run and Debug" -> Start Debugging.

    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}